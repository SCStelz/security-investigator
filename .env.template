# API tokens for enrich_ips.py
# Copy this file to .env and fill in your values, then source it before running:
#   PowerShell:  Get-Content .env | ForEach-Object { if ($_ -match '^([^#]\S+?)=(.*)$') { [System.Environment]::SetEnvironmentVariable($Matches[1], $Matches[2]) } }
#   Bash:        set -a; source .env; set +a

IPINFO_TOKEN=
ABUSEIPDB_TOKEN=
VPNAPI_TOKEN=
SHODAN_TOKEN=
